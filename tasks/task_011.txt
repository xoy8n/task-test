# Task ID: 11
# Title: Implement History Stack Management
# Status: pending
# Dependencies: 3
# Priority: high
# Description: Implement a mechanism to track canvas changes and store them in a history stack for undo/redo functionality.
# Details:
Add `history` (an array or stack structure) and `historyIndex` states to the store. Implement actions like `addHistoryEntry`, `undo`, `redo`. Each history entry should represent a state of the canvas (e.g., a serializable JSON representation from Fabric.js `canvas.toJSON()`). Manage the stack to allow navigation.

# Test Strategy:
Manually call `addHistoryEntry` with dummy data. Call `undo` and `redo`. Verify that the `historyIndex` and the content of the history stack are updated correctly according to stack logic (LIFO for undo, FIFO for redo from the current point).
